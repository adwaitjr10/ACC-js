Git init-create repo
Git status-code status i.e pushed/pull as per local repo
Git add--all-  -add new files
Git commit-am"<repo1>"  -to add specific features to specific repo
Git pull -restores the global repo to local repo
Git pushh -push the bunch that is been added by the add to the global repo
Git stash -code that will be in use for further use but not for the current version will be stored sepratly and wont be updated on the cloud
Git diff -diffrence between cloud rep and loacal repo

merge -conflicts between local and global version
branches -create multiple repo for the main version
delete branch
	git branch-m<old name><new name>
	git push origin:<old name><new name>
	git push -u origin<new name>

create branch
	git checkkout -b<><new branch><copy of branch>
	git add--all
	git commit -m<msg>-m<msg>
	git push -u origin<new name>
	git push origin<new branch>

